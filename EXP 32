#include <stdio.h>
#include <stdlib.h>

struct node{ int data; struct node *next; };
struct node *head=NULL,*temp,*ptr;

int main(){
    int v;
    temp=malloc(sizeof(struct node));
    scanf("%d",&v);
    temp->data=v; temp->next=head; head=temp;
    ptr=head; head=head->next; free(ptr);
    ptr=head; while(ptr){ printf("%d ",ptr->data); ptr=ptr->next; }
}
